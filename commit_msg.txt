feat: Integrate Dashboard Stats, Fix Access Constraints & Resolve UI Bugs

- **Backend Dashboard Integration:** Exposes a new `GET /api/scan/dashboard` endpoint and `getDashboardStats` controller to calculate and fetch live database parameters including scan counts, actual risk averages, and block threats.
- **Frontend Dashboard Wiring:** Bound real-time API routes to `DashboardHome.jsx` to eliminate static mock dependencies for the main visual UI structure.
- **Data Mapping Repairs:** Fixed empty risk percentages across `ScanHistory.jsx` and `Scan.jsx` generated by incorrectly evaluating objects as numerical arrays.
- **Prisma IP Insertion Crash Fix:** Safely handles empty metadata returns from the Flask ML microservice omitting undefined attributes that were violating PostgreSQL database string field strictness checks.
- **Routing Overhaul:** Transitioned the `Working.jsx` diagram canvas into a standalone unauthenticated `100vh` route; properly maintaining public rendering without height collapse bugs while keeping side-menu layout isolation intact.
- **Navbar Layout Refractions:** Enabled persistence of frontend sessions over page reloads explicitly handling context hooks, and added an immediate "Sign Out" interaction toggle.
